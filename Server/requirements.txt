websockets>=12.0
cryptography>=50.0.0
miniupnpc>=2.3.3

# Опционально: только если нужны push-уведомления через Firebase
# firebase-admin>=6.0

# Шифрование БД at-rest (см. DB_ENCRYPTION_KEY_HEX в .env.example).
# Без переменной DB_ENCRYPTION_KEY_HEX сервер работает и без неё, БД просто не шифруется.
sqlcipher3-binary>=0.6.0

# Опционально: только для генерации QR-картинок кодов доступа
# (Server/admin_gen_codes.py) — офлайн, без сети, ничего никуда не уходит.
# Без этих пакетов скрипт просто печатает ссылку текстом.
# qrcode[pil]>=7.4
